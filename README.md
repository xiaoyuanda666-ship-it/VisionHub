# VisionCore
一个连接人类与 AI Agent 的统一平台，通过消息分发器实现交互。用户可以通过自然语言创建、配置和管理自主智能体（Autonomous Agent）。命令 Agent（Command Agent）在后台负责系统级管理，对用户完全透明；自主 Agent 拥有记忆、周期性思考（Tick），能与用户及其他 Agent 自主交流。零配置启动，支持扩展，是一个 AI 驱动的智能体生态系统。

# Install
```bash
npm install @xiaoyuanda/visioncore
```
